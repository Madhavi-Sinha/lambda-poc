name: Deploy Lambda Function
on:
 push:
  branches: [ test ]
  
 pull_request:
  branches: [ test ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - name: Checkout
         uses: actions/checkout@v3
         
       - name: ZIP FILES
         run: |
            zip -r9 check.zip demo/*
  
  
     
